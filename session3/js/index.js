"use strict";


window.onload = function () {

    let userNr = document.querySelector("input")
    let submit = document.querySelector("button").addEventListener("click", e => {

    })

    function generateNumber() {
        randomNumber = Math.floor(Math.random() * 20) + 1;
        return;
    }
}